datasource postgres {
  provider = "postgresql"
  url      = env("DB_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  firstName String?
  lastName  String?
  username  String   @unique
  password  String
  roles     Json
}

model Employee {
  id              String     @id @default(cuid())
  createdAt       DateTime   @default(now())
  updatedAt       DateTime   @updatedAt
  employees       Employee[] @relation(name: "EmployeeOnEmployees")
  employee        Employee?  @relation(name: "EmployeeOnEmployees", fields: [employeeId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  employeeId      String?
  xsdaxsza        String?
  xcsxd           String?
  xsszxsx         String?
  rfuihriuhfi     String?
  hfelrhbflrohfrg String?
  krenmkVr        String?
  mjfrEkjmgrG     String?
  jmfLejmLfem     String?
  llvgRelkmv      String?
  mLkremvgLrem    String?
  vklrgemnvFer    String?
  nklrenK         String?
  nlkvnrgK        String?
  fgiuewh         String?
  hfljrebfl       String?
  nlfrnlr         String?
  vcvcbcbc        String?
  ncncnc          String?
  kcKC            String?
}

model UserTest {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  password  String?
  username  String?  @unique
}
